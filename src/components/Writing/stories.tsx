import { Story, Meta } from '@storybook/react/types-6-0';
import { Writing } from '.';

export default {
  title: 'Writing',
  component: Writing,
} as Meta;

export const Default: Story = () => <Writing />;
