import 'match-media-mock';
import { render } from 'utils/tests';

import { Themes } from '.';

describe('<Themes />', () => {
  it('should render correctly', () => {
    render(<Themes />);
  });
});
