import { Story, Meta } from '@storybook/react/types-6-0';
import { MobileMenuToggle } from '.';

export default {
  title: 'MobileMenuToggle',
  component: MobileMenuToggle,
} as Meta;

export const Default: Story = () => <MobileMenuToggle />;
